version: '3.8'
services:
  database:
    image: redis:alpine
    volumes:
      - dashboard_data:/data

  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    depends_on:
      - database

volumes:
  dashboard_data:
